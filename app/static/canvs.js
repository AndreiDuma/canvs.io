$(document).ready(function() {

//  $('canvas').width($(window).width());
  //$('canvas').height($(window).height());

  $('canvas').drawRect({
    fillStyle: '#000',
    x: 0, y: 0,
    width: 100,
    height: 100
  });

});
